services:
  - type: web
    name: dnd-battlemap
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: battlemap-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: DATA_DIR
        value: /data
